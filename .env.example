# OWNERSHIP: tooling
# Local overrides only. Shared credentials live in the hub workspace .env
# (C:\personal\naquuuu\.env) and are read by reference via os.getenv —
# never copy secret values into this repository.
#
# All values below are intentionally empty placeholders.
BI_SCRAPER_HUB_ENV=
BI_SCRAPER_DATA_DIR=
BI_SCRAPER_DB=
BI_SCRAPER_EXPORTS_DIR=
